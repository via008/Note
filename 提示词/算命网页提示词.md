“请帮助我实现一个本地运行的‘大模型算命’网页。以下是要求：

1. 网页前端：

- 使用HTML、CSS和JavaScript来实现页面。
- 页面有一个文本输入框，用户输入他们的问题（例如：“我今天的运势怎么样？”）。
- 页面上有一个提交按钮，用户点击后，问题通过JavaScript发送到本地的后端API。
- 用户点击提交后，网页会显示加载中状态，等待后端生成答案。

2. 后端：

- 在本地运行一个大模型（例如，OpenAI GPT-3、GPT-4或任何合适的模型）来生成回答。
- 通过一个简单的Node.js后端来接收请求，传递给大模型，获取答案并返回给前端。
- 可以通过简单的API调用来实现此功能。例如，使用Express.js来创建API。

3. 功能要求：

- 用户输入的问题通过POST请求发送到后端。
- 后端模型返回算命结果（例如：“今天你的运势不错，但需要小心投资”）。
- 前端展示返回的结果。
- 页面要有简单的美化，使界面友好清晰。

4. 实现：

- 前端：HTML文件包含输入框、按钮和显示区域，CSS文件用于简单的页面布局。
- 后端：Node.js+Express来处理请求，模型（例如GPT-4）用作回答生成器。

5. 其他要求：

- 页面只需要在本地运行，不需要部署到服务器上。


qwen:
export OPENAI_API_KEY="sk-9fbf94ae2fdb4810b77e9ffe86ac0071"
export OPENAI_BASE_URL="https://dashscope-intl.aliyuncs.com/compatible-mode/v1"
export OPENAI_MODEL="qwen3-coder-plus"