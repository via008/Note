请帮我使用 React Native 实现一个安卓记账本 App，可以在小米手机上本地运行，使用 Expo CLI 构建项目。以下是具体需求和技术要求：

【开发平台】
- 使用 React Native（基于 React）
- 使用 Expo CLI 进行开发和真机测试
- 最终可以导出 APK 文件安装到小米手机，无需连接后端，仅本地数据存储

【功能需求】
1. 添加账目：包括日期、类别（例如：餐饮、交通、购物等）、金额、备注。
2. 查看账目列表：展示所有账目记录，按日期排序。
3. 删除账目：用户可以删除某条账目。
4. 数据持久化：使用本地存储（AsyncStorage）保存数据，即使关闭 App 后数据依然保留。
5. 汇总功能：
   - 显示总支出金额
   - 按类别汇总支出金额

【UI 要求】
- 简洁现代的界面
- 使用 React Navigation 进行页面导航
- 样式尽量使用 Styled Components 或内联 StyleSheet
- 所有组件代码拆分清晰，便于后期扩展

【项目结构要求】
- 使用 TypeScript（如果复杂度高，可以先用 JS）
- 拆分组件（账单列表、添加表单、分类汇总等）
- 项目包含完整的 `App.js`、页面组件、样式文件、说明文档（README）

请使用模块化结构编写项目，并添加注释说明关键逻辑，帮助我理解代码。最终项目应可以通过 `npx expo start` 启动并在 Android 手机上运行。
